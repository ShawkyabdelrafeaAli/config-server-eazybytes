
server:
  port: 9000
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/cards
    username: root
    password: 682000
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

  cloud:
    config:
      enabled: false
