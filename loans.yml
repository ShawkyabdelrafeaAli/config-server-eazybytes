spring:
  datasource:
    url: jdbc:mysql://localhost:3306/loans
    username: root
    password: 682000
  jpa:
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQLDialect
        format_sql: true
    hibernate:
      ddl-auto: update
    show-sql: true

server:
  port: 8090
